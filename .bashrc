alias c=clear
alias ll="ls -la"
# use vi mode on bash ( ESC = ctrl [ )
# set -o vi

# History management
HISTCONTROL=ignoreboth          # don't store duplicates
HISTFILESIZE=10000              # store up to 10k lines

export PATH=$PATH:/root/go/bin